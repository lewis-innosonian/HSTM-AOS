<?xml version="1.0" encoding="utf-8"?>
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/tooltipRoot"
    android:layout_width="wrap_content"
    android:layout_height="wrap_content"
    android:padding="0dp"
    >

    <FrameLayout
        android:id="@+id/contentBox"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        >

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical"
            android:padding="40dp">

            <TextView
                android:id="@+id/tooltipText"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:textColor="#666666"
                android:textSize="16sp"
                android:lineSpacingExtra="4dp"
                android:text="1. Power on the device.\n2. Searching for devices automatically.\n3. Select a device from the list.\n4. If a manikin is connected, check that its light is on.\n    If AED-Trainer is connected, a sound will be heard." />

        </LinearLayout>

    </FrameLayout>

</FrameLayout>